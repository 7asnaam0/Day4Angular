import { HighlightIteamDirective } from './highlight-iteam.directive';

describe('HighlightIteamDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightIteamDirective();
    expect(directive).toBeTruthy();
  });
});
